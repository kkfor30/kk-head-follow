# 更新记录

## 0.3.0

- 正式发布；README 提供可复制给 Agent 的一句话安装指令。
- 按进入、完整环与退出分配时长，补充8–10秒规划建议及预算边界；上弧按实帧标定并保留接点连续性。

- 动作规格直接派生实际提交提示词；提供离线导出、输入绑定、完整环/中性进入/上弧/补段配置，保留手写入口。
- 明确一次只驱动一个角色，允许必要静态场景上下文；补齐原有参考/分段示例缺失的 subjects 字段。
- 增加浅色、深色、复杂背景与前景遮挡的选路，以及顶部、眼神、节奏、合成和运行时的定向修复步骤。
- 增加少量原生帧真实场景合成，支持样片遮罩与前景层；与编译器共享背景处理和羽化，保留 VFR 索引及来源。
- 阻止样片专用遮罩/前景配置被编译器静默忽略；统一编译与原生帧检查的旋转处理。
- 新增实际请求一致性、示例可执行性和深色/纹理/遮挡像素测试。完成深色人物场景生成与上弧修复实测，用户确认效果可用；不据单例宣称跨素材成功率提高。

## 0.2.1-experimental.1

- 统一补帧来源的 Python / 网页质量校验；使用已核验字节绘制底图与图集，恢复后退缓存生命周期。
- 编译采用独立输出与整套发布，失败不覆盖旧图集；保留 VFR 原生帧索引，修复无彩色色键崩溃。
- 生成前校验媒体格式和 MIME；遮罩哈希兼容 Python 3.10。
- 增加原生母版全帧快速检查及可行性决策，先定位动作缺口再精修；phase 不作为方向跟随完成。
- 新生成请求限定单主体并检查实际输入隔离；旧 spec 需补 motionPlan.subjects 与 subjectIsolation，旧任务查询不受影响。
- 新增最多 24 帧的初筛总览，头部/眼神分别记录；明确正脸表情兼容、二维注视与自然进出所需能力，单环不会自动具备这些控制。
- 增加实际媒体、跨语言校验与生命周期回归。未新增付费生成，不宣称生成成功率提高。

## 0.2.0-experimental.1

- 新增首尾帧、动作参考与分段生成路线，以及请求级输入审查。
- 增加模型能力与扩写控制、修复换参考及累计预算约束。
- 新增有限方向与动作相位候选预览，保留正式 ready 检查边界。
- 补充生成规划、评估指南与离线测试；尚未完成新的付费端到端生成验证。

## 0.1.0-experimental.1

首次公开实验版：连续视频方向标定、图集制作、网页运行时与质量检查工具链；包含公开效果录屏。自然回正与复杂遮挡仍需独立验证。
